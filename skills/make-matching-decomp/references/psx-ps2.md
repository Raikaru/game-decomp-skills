# PSX / PS2 Matching Notes

- Common tools: `splat` for supported MIPS binaries, `m2c`, `asm-differ`, `objdiff`, platform-specific disassemblers.
- Watch overlays, scratchpad usage, DMA-like hardware interactions, function boundary uncertainty, delay slots, and calling conventions.
- Record public documentation sources for register names.
- Keep disc images, executables, and extracted assets local-only.
