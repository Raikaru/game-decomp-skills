# Local Artifacts

Place user-owned local inputs in ignored directories:

- `artifacts-local/`
- `original-private/`
- `baserom-local/`

Generated outputs in `build/`, `expected/`, and scratch directories are ignored by default. Do not publish these outputs unless redistribution rights are explicit.
